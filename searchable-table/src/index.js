var React = require('react');
var ReactDOM = require('react-dom');
require('./index.css');

var element = <p>Ready to build React App!</p>;


ReactDOM.render(
  element,
  document.getElementById('root')
);
